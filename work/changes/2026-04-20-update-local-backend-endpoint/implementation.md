# Implementation

## What Changed

- added a small frontend config module for the contacts API base URL
- set the local default to `http://0.0.0.0:8010`
- kept the base URL overrideable with `VITE_CONTACTS_API_BASE_URL`

## Validation

- no behavioral tests were required for this configuration-only update
