# Request

## Source

Local note from `/home/henrique/Downloads/integrated_local_mode.md`.

## Signal

The repository needs an explicit integrated local development path for real service interaction against seeded local services.

## Evidence

- the desired stack includes Docker Compose, a Python backend, a seeded local database, and the Solid frontend
- the mode is intended for contract validation, flow debugging, and integration testing
- this path should be distinct from isolated mode and from the external backend contract

## Extracted Meaning

The next slice should make integrated local mode a first-class development surface for running the frontend against local backend services and seeded data.
